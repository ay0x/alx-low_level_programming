#include "main.h"
/**
 * _strlen - Entry point
 * Description: Displays the length of a string
 * @s: Characters from string
 * Return: The length of the string characters
*/

int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
